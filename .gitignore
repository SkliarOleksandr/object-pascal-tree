# Delphi compiler output
*.dcu
*.dcp
*.bpl
*.exe
*.dll
*.map
*.drc
*.rsm
*.tds

# Delphi IDE local state
*.local
*.identcache
*.projdata
*.tvsconfig
*.dsk
__history/
__recovery/
ModelSupport*/

# Build output dirs. out/ is the repository-wide throwaway: out/dcu/{win32,win64}
# holds every build's .dcu (see tests/build.bat), and nothing reads one between
# runs -- safe to delete, and the one path to skip in a backup.
out/
Win32/
Win64/
WinArm64/
Linux64/
OSX64/
OSXARM64/
Android*/
iOS*/

# Test artifacts
tests/output/
*.testresults.xml

# Working notes: current steps, corpus paths, in-flight plans. Deliberately
# untracked — this repository is public and the projects measured against it
# are not. Keep anything that names one of them here.
local/

# OS junk
Thumbs.db
.DS_Store

# VS Code workspace settings (machine-specific pastree.* paths)
.vscode/
*.log
